import { ScheduleSortPipe } from './schedule-sort.pipe';

describe('ScheduleSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleSortPipe();
    expect(pipe).toBeTruthy();
  });
});
