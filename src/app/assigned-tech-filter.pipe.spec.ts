import { AssignedTechFilterPipe } from './assigned-tech-filter.pipe';

describe('AssignedTechFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignedTechFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
