import { TechPoSortPipe } from './tech-po-sort.pipe';

describe('TechPoSortPipe', () => {
  it('create an instance', () => {
    const pipe = new TechPoSortPipe();
    expect(pipe).toBeTruthy();
  });
});
