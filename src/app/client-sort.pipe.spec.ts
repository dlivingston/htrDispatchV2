import { ClientSortPipe } from './client-sort.pipe';

describe('ClientSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientSortPipe();
    expect(pipe).toBeTruthy();
  });
});
