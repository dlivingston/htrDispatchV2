import { TicketIdSortPipe } from './ticket-id-sort.pipe';

describe('TicketIdSortPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketIdSortPipe();
    expect(pipe).toBeTruthy();
  });
});
