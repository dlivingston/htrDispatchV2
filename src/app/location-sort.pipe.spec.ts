import { LocationSortPipe } from './location-sort.pipe';

describe('LocationSortPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationSortPipe();
    expect(pipe).toBeTruthy();
  });
});
